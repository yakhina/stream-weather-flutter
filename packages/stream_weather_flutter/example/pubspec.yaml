name: stream_weather_flutter_example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=2.12.0"

dependencies:
  flutter:
    sdk: flutter
  stream_weather:
    path: ../../stream_weather
  stream_weather_flutter:
    path: ../../stream_weather_flutter

flutter:
  uses-material-design: true